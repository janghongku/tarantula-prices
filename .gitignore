# Python venv & cache
.venv/
__pycache__/
*.pyc

# 네이버 로그인 세션 프로필 (절대 커밋 금지 — 로그인 쿠키 포함)
.naver-profile/
*-profile/

# 수집 디버그 / 실험 산출물 (커밋 안 함)
debug_*.html
debug_*.json
state_*.json
scrape_err.log
ui.png
# 네이버 탐색용 실험 스크립트 (공개 안 함)
exp_*.py

# 로컬 도구 / 에디터 상태
.claude/
# 로컬 미리보기 검증용 (개인 환경)
verify_ui.py
# 공유용 생성물 (build_share.py로 언제든 재생성)
거미가격_공유.html
# 그룹핑 재생성 산출물 (species_groups.py groups/candidates 로 재생성)
groups.json
candidates_review.txt
# 합본 엑셀 — 바이너리라 커밋 안 함(make_xlsx.py로 로컬 생성, CSV가 원본 아카이브)
거미_가격_아카이브.xlsx
# 가격추이 그래프 PNG — 글 첨부용, 로컬 생성(make_graphs.py)
graphs/
# 변동 요약 카드 이미지 — 글 첨부용, 로컬 생성(make_cards.py)
가격변동_카드.png

# macOS
.DS_Store

# 네이버 예약작업 스크립트/로그 (로컬 전용)
naver_auto.sh
naver_auto.log
